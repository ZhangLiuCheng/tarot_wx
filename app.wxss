/**app.wxss**/
.bg-image {
  position: absolute;
  width: 100vw;
  height: 100vh;
  animation: bg 8s infinite;
}

@keyframes bg {
  0% {
    transform: scale(1);
  }

  50% {
    transform: scale(1.5);
  }

  100% {
    transform: scale(1);
  }
}